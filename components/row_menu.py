"""Row overflow menu (portal). Not used by the acceptance path."""

from pages.base_page import BasePage


class RowMenu(BasePage):
    """Portal-rooted row menu. Locators must not be scoped under the file row."""
